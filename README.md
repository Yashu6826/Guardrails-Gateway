# Guardrails Gateway - SentraGuard Lite

A minimal GenAI guardrails gateway that analyzes prompts and context documents for security risks including prompt injection, PII leakage, and RAG injection.

## 🚀 Quick Start

### Using Docker (Recommended)
```bash
docker compose up --build